import { StyleSheet } from "react-native";

export default styles = StyleSheet.create({
    Cardcontainer:{
        marginHorizontal:30,
        marginVertical:10,
        padding:10,
        justifyContent:'center',
        alignItems:'center',

    },
})

